{% extends 'base.html.twig' %}

{% block body %}

    <form style="width:40%; margin-left: auto; margin-right: auto; "  class="form-group" action="/organize/edit/{{ organize.activity_id }}" method="post">
        <label for="name">activity id</label>
        <input type="text" name="activity_id" class="form-control"  value="{{ organize.activity_id }}" required>

        <label for="starts_at">employee id</label>
        <input type="text" name="pupil_id" class="form-control" value="{{ organize.pupil_id }}" required>

        <input type="submit">
    </form>

{% endblock %}

